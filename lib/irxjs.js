import { Observable, Subject, ReplaySubject, from, of, range } from 'rxjs';
import { map, filter, switchMap } from 'rxjs/operators';

export let irxjs = function(){

    let Hello = function(){

    };
    
    return {
        hello: Hello
    }
};